// This script will run after the page loads
document.addEventListener('DOMContentLoaded', (event) => {
    console.log('Tibyan Everything is ready!');
    
    // You can add more interactive features here if needed
});

// Example: Add a click event to the logo
document.querySelector('.logo').addEventListener('click', () => {
    alert('Welcome to Tibyan Everything!');
});